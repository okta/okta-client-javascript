// This is a stub file to satisfy SPM's requirement that targets have at least one source file.
// The actual implementation (TokenStorageBridge.swift, TokenStorageBridge.m/h) is excluded
// from SPM builds and intended to be compiled via CocoaPods in production.
// During `swift test` in CI, this stub allows the module to be recognized while the
// React-dependent implementation is not compiled.
