[@okta/auth-foundation](../..) / [Networking](../index.md) / getHeader

# Function: getHeader()

> **getHeader**(`headers`): `string` \| `null`

Returns string value of a `www-authenticate` header.

## Parameters

| Parameter | Type |
| ------ | ------ |
| `headers` | `HeadersInit` |

## Returns

`string` \| `null`

## Remarks

`HeadersInit` allows for a few different representations of headers with different access patterns (.get vs [key])
