[@okta/auth-foundation](../..) / [Core](../index.md) / hasSameValues

# Function: hasSameValues()

> **hasSameValues**\<`T`\>(`a1`, `a2`, `strict?`): `boolean`

Utility function to verify two arrays contain the same items

## Type Parameters

| Type Parameter |
| ------ |
| `T` |

## Parameters

| Parameter | Type | Default value |
| ------ | ------ | ------ |
| `a1` | `T`[] | `undefined` |
| `a2` | `T`[] | `undefined` |
| `strict` | `boolean` | `true` |

## Returns

`boolean`

## Remarks

Requires array contain unique items
